# Using Annotations Only Approach (No XML Used)

Here we use Hibernate Framework as a JPA Implementation / Provider, and we use
only Java code with annotations alone, along with a custom `.properties` file
(to store DB connection related configuration details, in any format we desire)
; and **NO USAGE OF XML is done**.

We will not even have the "persistence.xml" file.
